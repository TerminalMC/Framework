# When a release is created, adds a comment to each included PR.
name: Z | Notify PRs on Release
permissions:
  contents: read
  pull-requests: write

on:
  release:
    types: [ published ]

jobs:
  tag-notification:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository under $GITHUB_WORKSPACE, so the workflow can access it.
      # https://github.com/actions/checkout
      - name: Checkout sources
        uses: actions/checkout@v6

      # Run the JS script.
      # https://github.com/actions/github-script
      - name: Run script notify-release
        uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const path = require('path')
            const script = require(
              path.join(process.env.GITHUB_WORKSPACE, '.github/workflows/scripts/notify-release.js')
            )
            await script({ github, context, core })
